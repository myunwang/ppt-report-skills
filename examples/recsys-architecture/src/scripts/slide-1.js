// 紧凑+居中方案：不追求铺满。chip 用克制的固定字号（配合已规范的短文案，
// 不会截断），整图按内容自然高度、在 slide 中部垂直居中（CSS 的 margin:auto 0）。
// 无逐卡极限计算、无 logo 异步重算 —— 简单确定，专业紧凑。
function initSlide1() {
  // 数据已规范（每 chip 短词 / 长项已拆 / 英文简写），固定字号即可不截断。
  // 此处留空：尺寸全部由 CSS 固定值控制，无需 JS 自适应。
}
